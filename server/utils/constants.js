TIMEOUT_MS = 30000; // 30 sedscon
WINDOW_MS = 60 * 1000; // 1 minute
RATE_LIMIT = 15;
MAX_TOKENS = 200;
FILE_SIZE_LIMIT = 5 * 1024 * 1024;  // 5MB limit

module.exports = { TIMEOUT_MS, WINDOW_MS, RATE_LIMIT, MAX_TOKENS, FILE_SIZE_LIMIT };
